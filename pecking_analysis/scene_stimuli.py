from __future__ import print_function, division
from pecking_analysis.stimulus_protocol import SoundProtocol, SoundFilter


class SceneSequenceProtocol(SoundProtocol):

    def __init__(self, manager, filters, preprocessors, postprocessors, call_type=None):

        super(SceneSequenceProtocol, self).__init__(manager,
                                                    filters,
                                                    preprocessors,
                                                    postprocessors)
        self.call_type = call_type

    def create_stimuli(self):

        for ii in xrange()
        stimuli =
        # Add inputs of call_type together with inputs of another type


